import { instance } from './config';

export const userApi = {
  // 관리자 로그인
  adminLogin: () => {return;},

  // 관리자 로그인 체크
  isLogin: () => {return;},

  // 설문 응답 유저 체크
  responseUserCheck: () => {return;}
};
