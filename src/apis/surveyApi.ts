import { instance } from './config';

export const surveyApi = {
  // 설문지 저장
  saveSurvey: () => {return;},

  // 설문지 수정
  updateSurvey: () => {return;},

  // 설문지 상태 변경 (토글 버튼 연동)
  changeSurveyStatus: () => {return;},

  //
  getSurveyList: () => {return;},
};
